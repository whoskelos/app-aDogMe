<!--Navbar-->
<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand fw-bold" href="{{ path('app_index') }}">
			<img src="/img/logo-adogme_2.png" alt="logo adogme" width="50">
			aDogMe
		</a>
		<button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavId" aria-controls="collapsibleNavId" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="collapsibleNavId">
			<ul class="navbar-nav ms-auto mt-2 mt-lg-0" id="enlacesNavbar">
				{% if is_granted('ROLE_ADMIN', app.user) %}
					<!-- Mostrar contenido para usuarios con rol ROLE_ADMIN -->
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_insertar_perro') }}">Insertar 🐶</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_user_index') }}">Usuarios 🙎</a>
					</li>
				{% endif %}
				{% if app.user %}
					<!-- Mostrar contenido para el usuario autenticado usuario -->
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_listado_perro') }}">Mis perros</a>
					</li>
				{% endif %}


				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">{{ app.user.userIdentifier }} <i class="fa-solid fa-right-from-bracket"></i></a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login')}}">Login🔒</i></i></a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
